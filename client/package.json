{
    "name": "client",
    "version": "1.0.0",
    "scripts": {
        "build": "parcel build index.html --no-source-maps --no-scope-hoist --no-content-hash && javascript-obfuscator dist/*.js -o dist/*.js",
        "watch": "parcel watch index.html --no-source-maps"
    },
    "dependencies": {
        "vldom": "^8.1.0",
        "vldom-default-directives": "^1.3.3"
    },
    "devDependencies": {
        "@parcel/transformer-sass": "^2.7.0",
        "parcel": "^2.7.0"
    },
    "workspaces": [
        "packages/*"
    ]
}
