{
    "name": "client",
    "version": "1.0.0",
    "scripts": {
        "start": "parcel index.html --no-source-maps",
        "build": "parcel build index.html --no-source-maps",
        "watch": "parcel watch index.html --no-source-maps"
    },
    "dependencies": {
        "vldom": "^8.1.0"
    },
    "devDependencies": {
        "@parcel/transformer-sass": "^2.7.0",
        "parcel": "^2.7.0"
    }
}
